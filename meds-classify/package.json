{
  "name": "pharma-meds-classify",
  "version": "1.0.0",
  "private": true,
  "description": "Local batch job: extract clean {medications, actions} from each consultation's free-text prescribedMeds using a LOCAL Ollama model (zero cost, no API key) and store the structured result on the Firestore document.",
  "main": "classify-meds.js",
  "scripts": {
    "dry-run":   "node classify-meds.js --dry-run --limit 25",
    "backfill":  "node classify-meds.js",
    "catch-up":  "node classify-meds.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  }
}
